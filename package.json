{
  "dependencies": {
    "@hookform/resolvers": "^5.2.1",
    "react-helmet-async": "^2.0.5",
    "react-hook-form": "^7.62.0",
    "zod": "^4.0.16"
  },
  "devDependencies": {
    "cheerio": "^1.1.2",
    "fast-glob": "^3.3.3"
  },
  "scripts": {
  "extract": "node pacotes-web/scripts/extract-packages.mjs",
  "dev": "npm --prefix pacotes-web run dev",
  "build": "npm --prefix pacotes-web run build",
  "preview": "npm --prefix pacotes-web run preview"
  }
}
